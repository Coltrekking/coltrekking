{
  "name": "coltrekking",
  "version": "1.0.0",
  "description": "",
  "main": "pages/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "private": true,
  "dependencies": {
    "firebase": "^12.12.0"
  },
  "devDependencies": {
    "vite": "^8.0.9"
  }
}
